# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'TestsSwift' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  pod 'Quick', '~> 5.0'
  pod 'Nimble', '~> 10.0'

  target 'TestsSwiftTests' do
    inherit! :search_paths

  end

  target 'TestsSwiftUITests' do
    # Pods for testing
  end

end
